services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - healthsphere

volumes:
  mongo_data:

networks:
  healthsphere:
    driver: bridge